#include <stdio.h>
/**
 *main - Entry point
 *
 *Return: 0 if success
 */
int main(void)

	char ch = 'a';
	int i = 0;

	while (i < 16)
        {
		if (i > 10)
		{
			putchar(i + 0);
		}
		else
		{
			putchar(i + 0);
			ch++;
		}
		i++;
	}
        putchar('\n');
    return (0);
}

