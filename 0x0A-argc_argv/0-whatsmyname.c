#include <stdio.h>
/**
 * main - program that prints its name followed by a nwe line
 * @argc: arguement count
 * @argv: array of pointers to argument strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argc = argc;
	printf("%s\n", *argv);
	return (0);
}

