#include <stdio.h>

/**
 * main - Entry point
 * @argc: arguments
 * @argv: an array pointing to arguments
 * Return: 0
 */

int main(int argc, char *argv[])
{
	argv = argv;
	printf("%d\n", argc - 1);
	return (0);
}

